<!DOCTYPE html>
<html class="page" lang="en">
<head>
  {% if page.url == '/' %}
    {% set isIndex = true %}
  {% else %}
    {% set titleSuffix = ' — Krachbau' %}
    {% set isIndex = false %}
  {% endif %}
  <title>
    {{ pageTitle or title }}{{ titleSuffix }}
  </title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ preview or site.description }}">
  <link rel="alternate" href="{{ data.paths.feed }}" type="application/rss+xml" title="Krachbau Podcast RSS feed">
</head>
<body class="page__body">
  {{ content | safe }}
</body>
</html>
